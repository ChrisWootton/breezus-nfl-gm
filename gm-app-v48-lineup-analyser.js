/* Breezus NFL GM v4.8 - optimized Lineup Analyst */
(function(){
'use strict';
if(window.__gm48Booted)return; window.__gm48Booted=true;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const safe=(f,d)=>{try{return f()}catch{return d}};
const num=v=>Number.isFinite(Number(v))?Number(v):0;
function roster(){return safe(()=>S.rosters.find(r=>String(r.owner_id)===String(S.me?.user_id))||S.mine,null)||safe(()=>S.mine,null)}
function raw(id){return safe(()=>window.player(id),null)||safe(()=>S.players?.[String(id)],{})||{}}
function pname(id){const p=raw(id);return safe(()=>window.pn(id),null)||p.full_name||[p.first_name,p.last_name].filter(Boolean).join(' ')||String(id)}
function pos(id){const p=raw(id),x=safe(()=>window.position(id),null)||p.position||'';return ['DST','D/ST','DEFENSE'].includes(String(x).toUpperCase())?'DEF':String(x).toUpperCase()}
function team(id){return String(raw(id).team||'FA').toUpperCase()}
function health(id){return String(safe(()=>window.playerStatus(id)?.label,null)||raw(id).status||'AVAILABLE').toUpperCase()}
function proj(id){return num(safe(()=>window.expectedPoints(id),0))}
function ids(){return [...new Set((roster()?.players||[]).map(String).filter(Boolean))]}
function starters(){return (roster()?.starters||[]).map(String).filter(x=>x&&x!=='0')}
function slots(){return (safe(()=>S.league?.roster_positions,[])||[]).map(String).filter(x=>!['BN','IR','TAXI'].includes(x.toUpperCase()))}
function bench(){const s=new Set(starters());return ids().filter(id=>!s.has(id))}
function unavailable(id){const h=health(id),p=raw(id);return /IR|OUT|PUP|SUSP|INACTIVE|DOUBTFUL/.test(h)||p.injury_status==='IR'||p.injury_status==='OUT'}
function eligible(id,slot){const p=pos(id);if(slot==='FLEX')return ['RB','WR','TE'].includes(p);if(slot==='SUPER_FLEX')return ['QB','RB','WR','TE'].includes(p);if(slot==='REC_FLEX')return ['WR','TE'].includes(p);if(slot==='RB/WR')return ['RB','WR'].includes(p);return p===slot}
function optimise(){const candidates=ids().map(id=>({id,v:proj(id),p:pos(id)})).filter(x=>x.p&&!unavailable(x.id));const ss=slots();let best={score:-Infinity,assign:[]};function walk(i,used,a,total){if(i>=ss.length){if(total>best.score)best={score:total,assign:a.slice()};return}const slot=ss[i];const cs=candidates.filter(x=>!used.has(x.id)&&eligible(x.id,slot)).sort((a,b)=>b.v-a.v);if(!cs.length){walk(i+1,used,a,total);return}for(const x of cs.slice(0,25)){used.add(x.id);a.push({id:x.id,slot});walk(i+1,used,a,total);a.pop();used.delete(x.id)}}walk(0,new Set(),[],0);return best}
function statusLabel(id){const h=health(id),v=proj(id);if(unavailable(id))return ['UNAVAILABLE','red'];if(v>=20)return ['ELITE START','green'];if(v>=15)return ['STRONG START','green'];if(v>=10)return ['START / MONITOR','amber'];return ['CONSIDER REPLACING','amber']}
function replacementMap(opt){const m={};opt.assign.forEach(x=>m[String(x.slot)]=x.id);return m}
function starterRows(opt){const a=starters(),ss=slots(),map=replacementMap(opt);return ss.map((slot,i)=>{const id=a[i]&&a[i]!=='0'?a[i]:null;const assigned=map[String(slot)];return {slot,id,assigned:assigned||id}})}
function gmDecision(row){if(!row.id)return {label:'EMPTY',cls:'amber',reason:'No player is currently assigned to this slot.'};if(unavailable(row.id)){return row.assigned&&String(row.assigned)!==String(row.id)?{label:'REPLACE',cls:'red',reason:`Replace ${pname(row.id)} with ${pname(row.assigned)}.`}:{label:'REPLACE',cls:'red',reason:`${pname(row.id)} is unavailable and no legal replacement is currently available.`};}if(row.assigned&&String(row.assigned)!==String(row.id)){const gain=proj(row.assigned)-proj(row.id);return {label:gain>=2?'REPLACE':'CONSIDER REPLACING',cls:gain>=2?'red':'amber',reason:`${pname(row.assigned)} projects ${proj(row.assigned).toFixed(1)} vs ${proj(row.id).toFixed(1)} for ${pname(row.id)}.`};}return {label:statusLabel(row.id)[0],cls:statusLabel(row.id)[1],reason:'This player is part of the best legal projected lineup.'}}
function card(row,opt){const d=gmDecision(row),id=row.id,rep=row.assigned&&String(row.assigned)!==String(id)?row.assigned:null;return `<div class="gm48-card"><div class="gm48-title"><div><span class="gm48-slot">${esc(row.slot)}</span><b>${id?esc(pname(id)):'Empty slot'}</b>${id?`<span class="gm48-meta">${esc(pos(id))} · ${esc(team(id))} · ${proj(id).toFixed(1)} projected · ${esc(health(id))}</span>`:''}</div><span class="gm48-pill gm48-${d.cls}">${esc(d.label)}</span></div><div class="gm48-reason">${esc(d.reason)}</div>${rep?`<div class="gm48-replace"><b>GM MOVE</b><br>${esc(pname(id))} → <strong>${esc(pname(rep))}</strong><br><span>${proj(rep).toFixed(1)} projected vs ${proj(id).toFixed(1)} · +${(proj(rep)-proj(id)).toFixed(1)}</span></div>`:''}</div>`}
function groupedBench(){const groups={QB:[],RB:[],WR:[],TE:[],DEF:[],OTHER:[]};bench().forEach(id=>{const p=pos(id);(groups[p]||groups.OTHER).push(id)});Object.keys(groups).forEach(k=>groups[k].sort((a,b)=>proj(b)-proj(a)));return groups}
function benchSection(){const g=groupedBench();let html='';['QB','RB','WR','TE','DEF'].forEach(p=>{const arr=g[p];html+=`<div class="gm48-position"><div class="gm48-poshead"><h3>${p}</h3><span>${arr.length} available</span></div>${arr.map((id,i)=>`<div class="gm48-bench"><span class="gm48-rank">${i+1}</span><div><b>${esc(pname(id))}</b><span class="gm48-meta">${esc(team(id))} · ${esc(health(id))}</span></div><strong>${proj(id).toFixed(1)}</strong></div>`).join('')||'<div class="gm48-muted">No current bench players.</div>'}</div>`});const unavailablePlayers=bench().filter(unavailable).sort((a,b)=>proj(b)-proj(a));if(unavailablePlayers.length)html+=`<div class="gm48-position gm48-unavailable"><div class="gm48-poshead"><h3>UNAVAILABLE / IR</h3><span>${unavailablePlayers.length}</span></div>${unavailablePlayers.map(id=>`<div class="gm48-bench"><span class="gm48-rank">•</span><div><b>${esc(pname(id))}</b><span class="gm48-meta">${esc(pos(id))} · ${esc(team(id))} · ${esc(health(id))}</span></div><strong>${proj(id).toFixed(1)}</strong></div>`).join('')}</div>`;return html}
function css(){if(document.getElementById('gm48-style'))return;const s=document.createElement('style');s.id='gm48-style';s.textContent=`#lineup .gm48-wrap{margin-top:10px}.gm48-hero{display:grid;grid-template-columns:1.5fr 1fr;gap:10px}.gm48-kpi{font-size:30px;font-weight:950;line-height:1}.gm48-sub{font-size:10px;color:#746e79;margin-top:5px}.gm48-card,.gm48-position{background:#fff;border:1px solid #e6e0da;border-radius:15px;padding:13px;margin-top:10px}.gm48-title,.gm48-poshead{display:flex;justify-content:space-between;align-items:center;gap:10px}.gm48-slot{background:#f0ece8;border-radius:7px;padding:4px 7px;font-size:9px;font-weight:950;margin-right:7px}.gm48-pill{padding:5px 8px;border-radius:99px;font-size:9px;font-weight:950;white-space:nowrap}.gm48-green{background:#e4faf3;color:#146b55}.gm48-amber{background:#fff1d1;color:#82590e}.gm48-red{background:#fdeaea;color:#9d2929}.gm48-meta{display:block;color:#746e79;font-size:10px;margin-top:3px}.gm48-reason{font-size:11px;color:#4f4951;margin-top:8px;line-height:1.4}.gm48-replace{margin-top:9px;padding:9px;border-radius:10px;background:#f7f4f1;font-size:11px;line-height:1.4}.gm48-replace span{color:#746e79}.gm48-position{padding:0;overflow:hidden}.gm48-poshead{padding:13px;background:#faf9f7}.gm48-poshead h3{margin:0}.gm48-poshead span{font-size:9px;color:#746e79;font-weight:900}.gm48-bench{display:flex;align-items:center;gap:9px;padding:10px 12px;border-top:1px solid #eee9e5}.gm48-bench>strong{margin-left:auto}.gm48-rank{width:18px;color:#746e79;font-size:10px;font-weight:900}.gm48-muted{font-size:11px;color:#746e79;padding:12px}.gm48-unavailable .gm48-poshead{background:#fff5f5}@media(max-width:850px){.gm48-hero{grid-template-columns:1fr}}`;document.head.appendChild(s)}
function render(){const el=document.getElementById('lineup');if(!el||!roster())return;css();const opt=optimise(),rows=starterRows(opt),current=rows.reduce((t,r)=>t+(r.id?proj(r.id):0),0),best=opt.score>-Infinity?opt.score:current,gain=Math.max(0,best-current),moves=rows.filter(r=>gmDecision(r).label==='REPLACE'||gmDecision(r).label==='CONSIDER REPLACING').length;const recommendations=rows.filter(r=>r.id&&gmDecision(r).label!=='ELITE START'&&gmDecision(r).label!=='STRONG START'&&gmDecision(r).label!=='START / MONITOR').map(r=>{const d=gmDecision(r);return `<div class="gm48-card"><span class="gm48-pill gm48-${d.cls}">${d.label}</span> <b>${esc(pname(r.id))}</b><div class="gm48-reason">${esc(d.reason)}</div></div>`}).join('');el.innerHTML=`<div class="gm48-wrap"><div class="gm48-hero"><div class="gm48-card"><div class="gm48-meta">GM RECOMMENDATION</div><div class="gm48-kpi">${moves?moves+' move'+(moves===1?'':'s'):'LINEUP OK'}</div><div class="gm48-sub">Current ${current.toFixed(1)} projected · Optimized ${best.toFixed(1)} · ${gain>0?'+'+gain.toFixed(1)+' available':'no projected gain'}</div></div><div class="gm48-card"><div class="gm48-meta">ROSTER</div><div class="gm48-kpi">${bench().length}</div><div class="gm48-sub">actual bench players</div></div></div><h2 style="margin-top:18px">Current Lineup Analysis</h2>${rows.map(r=>card(r,opt)).join('')}<div style="margin-top:20px"><h2>GM Recommendations</h2>${recommendations||'<div class="gm48-card"><b>No immediate changes recommended.</b><div class="gm48-reason">Your current starters are in the best projected legal lineup.</div></div>'}</div><div style="margin-top:20px"><h2>Actual Bench by Position</h2><div class="gm48-meta">Each position is sorted by projected points. Unavailable and IR players are separated at the bottom.</div>${benchSection()}</div></div>`}
function boot(){render();let last='';setInterval(()=>{const r=roster(),sig=JSON.stringify({players:r?.players||[],starters:r?.starters||[],positions:safe(()=>S.league?.roster_positions,[])||[],week:S.week});if(sig!==last){last=sig;render()}else if(document.getElementById('lineup')&&!document.querySelector('#lineup .gm48-wrap'))render()},1500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
